import request from 'supertest';
import assert from 'assert';
import app from '../../../../';

const server = app.createServer();

describe('TEST GET /leads/score', () => {
    it('define a testcase here', (done) => {
        done();
    });
});